# 당구 "파이브앤하프" 계산기/앱 — 프로젝트 지침 (CLAUDE.md)

당구 **파이브앤하프(다이아몬드) 시스템**으로 3쿠션 경로·당점·세기를 계산해 보여주는
"**당구장 컨닝페이퍼**". 최종은 AI 스마트안경, **지금은 Python 계산기부터** 단계적으로.

---

## 협업 규칙 (반드시 준수)
- **작업 전 자세히 설명 → 승인 후 진행.** 승인 후엔 중간확인 없이 끝까지.
- **지시 이외 임의 추가·변경 금지.** 요청한 것만.
- **거짓말 금지.** 모르면 모른다고.
- git 커밋/푸시는 사용자가 요청할 때. (아직 배포/서버 없음)

## 역할 분담 (중요)
- **사용자(유동주)** = 당구 **500점** 실력자, 도메인 전문가.
  당구 **시스템 값·레일 번호표·보정 계산법·당점·세기 기준**을 제공/검증한다.
- **Claude** = 좌표·계산·렌더링 **코드**만 구현한다.
  ⚠️ **당구 이론을 임의로 지어내지 말 것.** 사용자가 준 값을 정확히 코드로 옮긴다.
  (도메인 값의 진실 원천 = 사용자)

## 스크린샷 경로
- 전체화면: `C:\sc\숫자.jpg`
- 부분캡처: `C:\sc\ss.jpg`

---

## 로드맵 (위험 낮은 순)
- **0단계 (지금)**: Python + pygame — **두뇌(파이브앤하프 계산) + 2D 당구대 시각화**
- **0.5단계**: OpenCV — 정적 사진 원근보정(호모그래피)에서 공 검출
- **1단계**: 폰 캡처 방식 앱(원근보정+공검출+계산+오버레이) — **Unity + C#** 유력
- **2단계**: 폰 실시간 AR → **3단계**: 스마트안경(최종)

> 어려운 건 CV·안경이고 **두뇌는 규칙기반이라 쉽다.** 두뇌부터 만들고 나중에 얹는다.

## 기술
- 0단계: **Python + pygame** / CV: **OpenCV** / 앱: **Unity + C#**(안드로이드 네이티브보다 유리) / 도구: Claude Code

---

## 파이브앤하프 공식 (확정)
- **3쿠션수 = 수구 출발수 − 1쿠션수**  (확인 예: 출발 50 − 1쿠션 40 = 도착 10, 당점 3팁)
- **보정(correction)은 별도 계산법** → 사용자 규칙을 `correction` 모듈에 플러그인
- **레일 번호표(다이아몬드 번호↔위치)** 는 `data/`에 분리, 사용자가 채움/검증

### 계산 흐름
1. 공 클릭 → 수구·목적구 좌표
2. 수구→출발수, 목표→3쿠션수
3. **1쿠션수 = 출발수 − 3쿠션수** → 1쿠션 좌표
4. 보정 적용
5. 경로선 + 당점 + 세기 표시

## 규격 & 좌표계
- **중대 기준.** 쿠션 안쪽 규격 **2540 × 1270 mm** ← ⚠️ 임시값, 사용자 확인 필요
- 단위 mm, 원점 = 좌하단 코너, x=장축·y=단축
- 다이아몬드: 장쿠션 8등분·단쿠션 4등분

## 프로젝트 구조
```
billiards/
├─ main.py               # 실행 엔트리
├─ core/
│   ├─ table.py          # 규격·좌표계·다이아몬드 번호↔좌표
│   ├─ five_half.py      # 파이브앤하프 계산
│   ├─ correction.py     # 보정(사용자 규칙 플러그인)
│   └─ geometry.py       # 좌표·반사·경로 유틸
├─ ui/app.py             # 당구대·공 클릭·경로/당점/세기 표시
├─ data/
│   ├─ table_medium.json # 중대 규격
│   └─ five_half_numbers.json  # 레일별 번호표(사용자가 채움)
└─ CLAUDE.md
```
**원칙**: 도메인 값(규격·번호표·보정표)은 `data/`에 분리 → 값만 고치면 코드 수정 없이 튜닝.

---

## 첫 작업 (Task 0)
1. 위 구조로 뼈대 생성
2. `core/table.py` — 중대 규격·좌표계·다이아몬드 위치
3. `ui/app.py` — **pygame 당구대 + 다이아몬드 번호 + 공 클릭 배치**
4. `main.py` 실행
5. **계산은 그다음** — 우선 "당구대 그리고 공 놓기"까지

## 시작 전 사용자에게 확인
1. 중대 쿠션 안쪽 규격 (2540×1270 맞나)
2. 파이브앤하프 레일별 번호표 (수구 출발수 / 1쿠션 / 3쿠션 도착수)
3. 보정 계산법 명세
4. 당점 표기(팁)·세기 척도
