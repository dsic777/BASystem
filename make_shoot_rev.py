"""역회전(역각) 촬영 계획을 그림으로 만든다 → C:\\sc\\촬영R1.png · 촬영R2.png

    python c:\\Portfolio\\billiards\\make_shoot_rev.py

2026-08-13. 3차실사 세 영상을 분석해 178접점을 얻었는데 **전부 순회전**이었다.
역각은 아직 사용자 경험치 하나뿐이라, 그것만 채우러 간다.

★ 예전에 'B 역회전' 을 한 번 설계했다가 사용자 지적으로 뺐다 (촬영계획.md) —
  '변수가 넷(다이 · 속도 · 수구쿠션 거리 · 입사각)인데 20샷이면 표가 아니라 흩어진 점이다.'
  → 이번에는 **회전과 배치를 못 박고 세기만 바꾼다.** 변수를 둘로 줄인다.

⚠️ 그림 안에 ⚠️ 와 체크표시와 빼기표시를 쓰지 말 것. malgun.ttf 에 없어서 깨진다.
   ★ ※ ▶ ① → × · 는 잘 나온다.
"""
import re
import sys
from pathlib import Path

HERE = Path(__file__).resolve().parent
sys.path.insert(0, str(HERE))

# make_shoot.py 는 import 하면 옛 그림을 다시 만들어 버린다 (main 가드가 없다).
# 그래서 '촬영0' 정의 앞까지만 떼어 와서 Page · Mini · f · 색을 그대로 쓴다.
_src = (HERE / "make_shoot.py").read_text(encoding="utf-8")
_head = _src[: _src.index("# ══════════════════════════════════════════════ 촬영0")]
exec(compile(_head, "make_shoot.py(head)", "exec"))          # noqa: S102

OUT = Path(r"C:\sc")


# ───────────────────────────────────────────── 찍을 배치 (진실 원천)
#   앱 좌표: 원점 좌하단 · 장축 x 2448 · 단축 y 1224 (중대 쿠션 안쪽)
#   둘 다 좌상향으로 올려 친다 → 상단 쿠션에서 공이 흐르는 쪽은 **왼쪽**.
#   그래서 **오른쪽(3시) 회전** 이 역회전이 된다.
LAYOUTS = [
    dict(name="① 얕게", cue=(1400, 150), aim=(900, 1224), ain=25,
         note="입사 25도 언저리"),
    dict(name="② 눕게", cue=(2100, 300), aim=(800, 1224), ain=55,
         note="입사 55도 언저리"),
]

SET_E = dict(
    title="E  역각 — 남은 힘이 역회전을 얼마나 죽이나", shots=30, file="E_역각.mp4",
    head=["", "약", "중", "강", "파일"],
    rows=[["① 얕게  (입사 25도)", "5샷", "5샷", "5샷", "E_역각.mp4"],
          ["② 눕게  (입사 55도)", "5샷", "5샷", "5샷", "E_역각.mp4"]],
    w=[380, 165, 165, 165, 245],
)
SET_F = dict(
    title="F  3팁 채우기 — 지난 영상은 유효 7샷뿐이었다", shots=15, file="F_3팁.mp4",
    head=["", "약", "중", "강", "파일"],
    rows=[["3시 3팁 · 빈쿠션", "5샷", "5샷", "5샷", "F_3팁.mp4"]],
    w=[380, 165, 165, 165, 245],
)


def table(p, s):
    p.h(s["title"] + "      " + str(s["shots"]) + "샷", HOT)
    p.grid(s)


# ══════════════════════════════════════════════ 촬영R1
def pageR1():
    p = Page("당구장 촬영 — 역각 하나만", "2026-08-13")

    p.h("왜 가나", BLU)
    p.t("3차실사 세 영상을 다 분석했다. 178접점이 나왔는데 × 전부 순회전(벌어짐)이다.", INK, 30)
    p.t("역각(좁아짐) 자료는 한 접점도 없다. 앱의 역각은 아직 유동주님 경험치 하나에 서 있다.", INK, 30)
    p.gap(8)
    p.t("지금 앱이 쓰는 규칙 (v246)", HOT, 30)
    p.t("     ▶  남은 힘 3포인트 이상 → 역각 50%        ▶  1포인트 → 입사각 = 반사각", HOT, 30)
    p.t("이번 촬영으로 이 두 점을 실측으로 바꾼다. 그것 하나만 하러 간다.", DIM, 28)

    p.rule()
    p.h("★ 예전에 한 번 뺐던 항목이다 — 이번엔 변수를 줄였다", RED)
    p.t("유동주님 지적 (촬영계획.md) ▶  '변수가 넷인데 20샷이면 표가 아니라 흩어진 점 스무 개다.'", DIM, 28)
    p.t("다이 특성 · 속도 · 수구쿠션 거리 · 입사각   →   넷 다 움직였다.", DIM, 28)
    p.gap(6)
    p.t("이번에는 회전(3시 2팁)과 배치 두 개를 못 박는다. 움직이는 것은 세기 하나다.", INK, 30)
    p.t("배치가 고정이면 수구쿠션 거리도 고정이고, 같은 다이라 다이 특성도 고정이다.", INK, 30)

    p.rule()
    table(p, SET_E)
    p.t("한 배치에서 약 5샷 · 중 5샷 · 강 5샷. 두 배치니 30샷이다.", DIM, 28)
    p.t("★ 공을 하나만 놓는다. 빈쿠션이라야 분리각이 안 섞인다.", HOT, 29)
    p.t("★ 끝까지 굴러가게 둔다. 멈출 때까지 손대지 않는다 → 쿠션마다 남은 힘을 잰다.", HOT, 29)

    p.gap(10)
    table(p, SET_F)
    p.t("지난 '3순위_당점 2시30분 3팁' 은 유효 샷이 7개뿐이라 못 썼다. 15샷이면 채워진다.", DIM, 28)
    p.t("이건 순회전이라 배치를 안 정한다. 편한 대로 빈쿠션만 치면 된다.", DIM, 28)

    p.rule()
    p.h("공통 — 지난번과 똑같이", GRN)
    p.t("① 카메라 고정. 네 코너가 다 보이게. 자리는 지난번 그대로 (좌측하단 위 옷걸이)", INK, 29)
    p.t("② 한 샷이 완전히 멈춘 뒤 다음 샷. 구르는 중에 손이 들어오면 안 된다", INK, 29)
    p.t("③ 세기가 바뀔 때 카메라에 손가락으로 약 1 · 중 2 · 강 3", INK, 29)
    p.t("④ 두께 · 당점 · 각도는 따로 적지 않는다. 궤적에서 계산된다", INK, 29)
    p.t("⑤ 숫자를 정확히 맞추려 애쓰지 않는다. 세 무리로 갈라지기만 하면 된다", INK, 29)
    p.save("촬영R1.png")


# ══════════════════════════════════════════════ 촬영R2
def pageR2():
    p = Page("E 배치 두 개", "역회전 · 빈쿠션 · 공 하나")

    p.h("무엇이 역회전인가 — 헷갈리면 이 그림만 보면 된다", BLU)
    p.t("둘 다 왼쪽 위로 올려 친다. 상단 쿠션에서 공은 왼쪽으로 흐른다.", INK, 30)
    p.t("그 반대인 오른쪽 회전 ▶ 3시 2팁 을 준다. 이것이 역회전이다.", HOT, 32)
    p.gap(6)

    p.t("빨간선처럼 더 좁게 꺾이면 역각이 걸린 것이고, 점선(거울)대로 가면 안 걸린 것이다.", DIM, 28)
    p.gap(54)

    y = p.y
    for i, L in enumerate(LAYOUTS):
        mw = 565
        ox = 90 + i * (mw + 130)
        p.d.text((ox + mw / 2, y - 12), L["name"] + "   " + L["note"],
                 font=f(32, True), fill=HOT, anchor="ms")
        m = Mini(p, ox, y + 16, mw)
        cx, cy = L["cue"]
        ax, ay = L["aim"]
        dx, dy = ax - cx, ay - cy
        m.line([L["aim"], (ax + dx * 0.95, ay - dy * 0.95)], DIM, 4, dash=True)
        m.line([L["aim"], (ax + dx * 0.42, ay - dy * 0.95)], RED, 6)
        m.line([L["cue"], L["aim"]], HOT, 6)
        m.ball(cx, cy, "#f2f0e8")
        m.label(cx + 190, cy - 40, "수구", INK, 27)
        p.d.text((ox + mw / 2, m.bottom + 26),
                 "노란선 치는 길   ·   빨간선 역각이 걸린 길   ·   점선 거울",
                 font=f(23), fill=DIM, anchor="ma")
    p.y = y + 440

    p.rule()
    p.h("세기 세 가지 — 이것이 이번 촬영의 전부다", HOT)
    p.t("약 ▶ 3쿠션쯤에서 힘이 빠지게. 공이 네 번째 쿠션을 못 넘길 정도", INK, 30)
    p.t("중 ▶ 평소 파이브앤하프 칠 때의 세기", INK, 30)
    p.t("강 ▶ 다섯 쿠션 이상 살아서 도는 세기", INK, 30)
    p.gap(8)
    p.t("같은 배치를 세기만 바꿔 치면, 같은 쿠션에서 남은 힘만 다르다.", DIM, 28)
    p.t("그 쿠션의 반사각이 세기에 따라 얼마나 벌어지고 좁아지는지가 곧 역각 곡선이다.", DIM, 28)

    p.rule()
    p.h("찍고 나서", GRN)
    p.t("영상을 C:\\DSLINE\\영상편집 당구\\ 아래 아무 데나 두고 경로만 알려주시면 됩니다.", INK, 30)
    p.t("track.py 로 돌려서 쿠션마다 입사 · 반사 · 속도를 뽑고, 남은 힘 대 역각으로 표를 만듭니다.", INK, 30)
    p.t("코너 눈금은 처음 한 번만 찍으면 되고, 카메라를 안 옮겼으면 그것도 필요 없습니다.", DIM, 28)
    p.save("촬영R2.png")


pageR1()
pageR2()
